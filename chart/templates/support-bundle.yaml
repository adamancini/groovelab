apiVersion: v1
kind: Secret
metadata:
  name: {{ include "groovelab.fullname" . }}-support-bundle
  labels:
    {{- include "groovelab.labels" . | nindent 4 }}
    troubleshoot.sh/kind: support-bundle
type: Opaque
stringData:
  support-bundle.yaml: |
    {{- include "groovelab.supportBundle.spec" . | nindent 4 }}
