.vault/
.claude/
.worktrees/
.codebase/
.opencode/

# Go
/backend/bin/
/backend/server

# Node / frontend
/frontend/node_modules/
/frontend/dist/
/node_modules/

# Helm
chart/charts/
chart/Chart.lock
chart/*.tgz

# Makefile chart-package output
/dist/

# Packaged chart tarball dropped into release/ during release-unstable
# (co-located with KOTS CRs so `replicated release create --yaml-dir release/`
# picks up both). Should not be committed.
release/*.tgz

release-review-*.md

# Paivot runtime state (managed by pvg)
.vault/issues/
.vault/.nd.yaml
.vault/.piv-loop-state.json
.vault/.piv-loop-snapshot.json
.vault/.dispatcher-state.json
.vault/.sessionname-state.json
.vault/.vlt.lock
.vault/.guard/
